# データ移行手順

1. 旧FocusサイトをiPhoneで開く。
2. 下部の「データを書き出す」を押す。
3. `focus-backup-YYYY-MM-DD.json` をiCloud Driveへ保存する。
4. 新しいFocusサイトをSafariで開く。
5. 「データを読み込む」を押し、保存したJSONを選ぶ。
6. タスク表とカレンダーの記録を確認する。
7. Safariの共有ボタンから「ホーム画面に追加」を選ぶ。
8. Macでも使う場合は、同じJSONをMac側の新サイトで読み込む。

## 注意

端末間のリアルタイム同期は行いません。iPhoneとMacで別々に編集した場合は、自動では統合されません。現在の使い方ではiPhoneを主端末にし、Macは必要なときに最新バックアップを読み込む運用が安全です。
